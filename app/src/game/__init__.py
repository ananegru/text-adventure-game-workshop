"""Backend-owned game package (``src/game``).

Owns the room graph, inventory, item dependencies, command parsing, and the
win condition — everything the Squad's `backend` role implements from
``specs/speckit/spec.md`` and ``specs/speckit/tasks.md`` (T001–T005).
"""
