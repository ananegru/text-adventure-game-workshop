"""Frontend-owned UI package (``src/ui``).

Owns everything the player sees and types: the command prompt, response
rendering, room/inventory display, and user-facing edge-case messages. The
Squad's `frontend` role implements this from ``specs/speckit/spec.md`` and
``specs/speckit/tasks.md`` (T006–T008).
"""
